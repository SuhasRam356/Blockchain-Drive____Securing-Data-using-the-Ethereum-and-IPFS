# Deployment Rules

- Always check for an existing contract deployment in the `.env` file before running deployment scripts.
- Ask for explicit user permission before overwriting any existing contract addresses so the user does not lose their files.
